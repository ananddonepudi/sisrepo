apiVersion: v1
kind: Pod
metadata:
  
  name: nginx
  namespace: default

  labels:
   type: back-end
spec:
  containers:
  - image: nginx:1.14.2
    imagePullPolicy: IfNotPresent
    name: nginx
    ports:
    - containerPort: 80
      protocol: TCP

 